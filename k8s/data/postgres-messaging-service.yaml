apiVersion: v1
kind: Service
metadata:
  name: postgres-messaging
  namespace: groupchat
spec:
  type: ClusterIP
  selector:
    app: postgres-messaging
  ports:
    - port: 5432
      targetPort: 5432
