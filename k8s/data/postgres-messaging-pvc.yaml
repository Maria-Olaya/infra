apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-messaging-pvc
  namespace: groupchat
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: auto-ebs-sc
