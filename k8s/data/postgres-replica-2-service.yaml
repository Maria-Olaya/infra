apiVersion: v1
kind: Service
metadata:
  name: postgres-replica-2
  namespace: groupchat
spec:
  selector:
    app: postgres-replica-2
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
