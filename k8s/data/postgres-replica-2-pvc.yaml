apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-replica-2-pvc
  namespace: groupchat
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path
